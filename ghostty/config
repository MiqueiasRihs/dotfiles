# ==========================================
# APARÊNCIA
# ==========================================
theme = Ayu
font-family = "JetBrainsMono Nerd Font Mono"
font-size = 10

# Ligatures
font-feature = calt
font-feature = liga

background-opacity = 0.9
background-blur-radius = 20

# ==========================================
# COMPORTAMENTO
# ==========================================
cursor-style = block

cursor-style-blink = true
mouse-hide-while-typing = true

keybind = ctrl+l=clear_screen

# ==========================================
# JANELA E TAMANHO
# ==========================================
# Configuração corrigida (auto ou client)
window-decoration = auto

# Confirmação ao fechar (false = não pergunta)
confirm-close-surface = false

# TAMANHO PADRÃO (Colunas x Linhas)
# 130 colunas é ótimo para dividir a tela (split) e codar
# 40 linhas dá uma boa altura vertical
window-width = 125
window-height = 35

# ==========================================
# ATALHOS
# ==========================================
# Colar sem Shift
keybind = ctrl+v=paste_from_clipboard

# ==========================================
# ATALHOS DE DIVISÃO
# ==========================================
# Dividir para a DIREITA (Right)
keybind = ctrl+shift+right=new_split:right

# Dividir para BAIXO (Down)
keybind = ctrl+shift+down=new_split:down

# Fechar o painel atual (Close)
keybind = ctrl+shift+w=close_surface

# ==========================================
# NAVEGAÇÃO ENTRE PAINÉIS
# ==========================================
# Usando alt + Shift + Setas para mudar o foco
keybind = alt+shift+left=goto_split:left
keybind = alt+shift+right=goto_split:right
keybind = alt+shift+up=goto_split:top
keybind = alt+shift+down=goto_split:bottom

# ==========================================
# REDIMENSIONAR PAINÉIS
# ==========================================
# Alt + Setas para aumentar/diminuir
keybind = alt+up=resize_split:up,10
keybind = alt+down=resize_split:down,10
keybind = alt+left=resize_split:left,10
keybind = alt+right=resize_split:right,10

command = /usr/bin/zsh